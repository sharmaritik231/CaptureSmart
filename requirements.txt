# Core ML and tensor operations
torch==2.6.0
torchvision==0.21.0
numpy>=1.26.4

# Streamlit UI
streamlit==1.22.0
streamlit-webrtc==0.62.4

# Image handling (headless build, no libGL dependency)
opencv-python-headless==4.11.0.86
Pillow>=8.0.0

# (optional) progress bars in training scripts
tqdm>=4.60.0
